A Python-based GUI application built using Tkinter and SQLite3 that simulates a movie rental store.
Features include:

Movie catalog filtering (by genre, rating, keyword)

Customer rental history tracking

SQLite-powered persistent storage

Modular architecture (main.py, db.py, objects.py)

GUI design using Tkinter Treeviews & Widgets

Rental management per customer email
